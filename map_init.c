// # include "fdf.h"

// static void		zero_pt(t_pt *point)
// {
// 	point->x_pt = 0;
// 	point->y_pt = 0;
// 	point->z_pt = 0;
// }









// int		map_init(char **strmap)
// {
// 	char **line_ints;

// 	while (*strmap)
// 	{
// 		line_ints = ft_strsplit(*strmap, ' ');

// 		(*strmap)++;
// 	}
// }	

// t_pt	**map_init(char **strmap)
// {
// 	int rows;
// 	int cols;

// 	rows = ft
// 	t_pt **map;

// 	map = (t_pt **)ft_memalloc((sizeof(t_pt *) * rows + 1));

// }

	// char *line;
	// int fd;
	// int i = 0;

	// fd = open(file, O_RDONLY);
	// while (get_next_line(fd, &line) == 1)
	// {
	// 	if (!(validate_line(line)))
	// 	{
	// 		ft_err_fd(2);
	// 		return (0);
	// 	}
	// 	*(strmap + i) = ft_strdup(line);
	// 	//ft_putstr(*(strmap + i));
	// 	i++;
	// 	free(line);
	// }
	// *(strmap + i) = 0;
	// ft_putstrmap(strmap);
	// return (1);